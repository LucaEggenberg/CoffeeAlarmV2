[Unit]
Description=GPIO Web-API
After=multi-user.target

[Service]
Type=idle
ExecStart=/usr/local/bin/uvicorn --app-dir /bin/api main:app --reload --host=0.0.0.0 --port=8000

[Install]
WantedBy=multi-user.target